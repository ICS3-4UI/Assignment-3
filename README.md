# Assignment-3 (Graphics Unit)

There are two Pacman script, one is using loops to animate, another one is using OOP style and uses Tkinter's built-in methods to prevent the character from flickering.
